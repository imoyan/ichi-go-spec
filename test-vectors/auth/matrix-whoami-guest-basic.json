{
  "name": "matrix-whoami-guest-basic",
  "contract": "SPEC-032",
  "issue": "imoyan/houra-spec#411",
  "given": {
    "previous_request": {
      "method": "POST",
      "path": "/_matrix/client/v3/register",
      "query": {
        "kind": "guest"
      },
      "body": {
        "username": "ignored-guest-name",
        "password": "ignored password",
        "device_id": "IGNORED",
        "initial_device_display_name": "Guest preview"
      }
    }
  },
  "request": {
    "method": "GET",
    "path": "/_matrix/client/v3/account/whoami",
    "access_token": "token-guest"
  },
  "expected": {
    "status": 200,
    "body_contains": {
      "user_id": "@guest1:example.test",
      "device_id": "GUEST1",
      "is_guest": true
    },
    "versions_advertisement_widened": false,
    "excluded_breadth": [
      "room preview event stream",
      "guest-specific rate-limit policy",
      "guest-specific API allowlist breadth beyond representative createRoom rejection"
    ]
  }
}
